# Done

Completed roadmap items that did not have a dedicated project write-up.

## 2026-02-27

- Added `outputColorMode` to the `Basic` runtime effect (`rgb`, `yuv`, `hsv`, `lab`) for immediate visual output-mode changes.
- Kept `colorSpace` behavior as the processing-space selector to preserve legacy `Basic` effect behavior in existing hypnographs.
